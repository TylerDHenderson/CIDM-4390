@page
@model WebApp1.Pages.ProgramAdmins.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProgramAdmin[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgramAdmin[0].LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProgramAdmin) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProgramAdminID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProgramAdminID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProgramAdminID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
